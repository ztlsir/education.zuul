server:
  port: 805

spring:
  application:
    name: zuul
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:803/eureka/